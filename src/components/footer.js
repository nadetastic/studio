import React from "react";
import { Footer as UIFooter } from "../ui-components";

export default function Footer() {
  const overrides = {};

  return <UIFooter width={"100vw"} overrides={overrides}></UIFooter>;
}
